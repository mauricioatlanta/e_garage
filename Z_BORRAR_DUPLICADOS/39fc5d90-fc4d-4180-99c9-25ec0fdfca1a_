{% extends 'base.html' %}
{% load static %}
{% load humanize %}

{% block title %}Repuestos | e-Garage{% endblock %}

{% block content %}
<div class="max-w-5xl mx-auto mt-10 translucent-card/90 p-8 rounded-xl shadow-lg">
  <h1 class="text-3xl font-bold text-gray-800 mb-6">Dashboard de Repuestos</h1>
  <p class="mb-4 text-gray-600">Aquí puedes gestionar y visualizar los repuestos disponibles en el sistema.</p>
  <div class="overflow-x-auto">
    <table class="min-w-full text-sm text-left text-gray-700">
      <thead class="translucent-card text-xs uppercase">
        <tr>
          <th class="px-6 py-3">Nombre</th>
          <th class="px-6 py-3">Código</th>
          <th class="px-6 py-3">Stock</th>
          <th class="px-6 py-3">Precio</th>
          <th class="px-6 py-3">Acciones</th>
        </tr>
      </thead>
      <tbody>
        {% if repuestos %}
          {% for repuesto in repuestos %}
          <tr class="border-b hover:bg-gray-50 transition">
            <td class="px-6 py-4 font-medium">{{ repuesto.nombre }}</td>
            <td class="px-6 py-4">{{ repuesto.codigo }}</td>
            <td class="px-6 py-4">{{ repuesto.stock }}</td>
            <td class="px-6 py-4">${{ repuesto.precio }}</td>
            <td class="px-6 py-4 space-x-2">
              <a href="#" class="text-blue-600 hover:text-blue-800" title="Ver"><i class="fas fa-eye"></i></a>
              <a href="#" class="text-yellow-500 hover:text-yellow-600" title="Editar"><i class="fas fa-edit"></i></a>
              <a href="#" class="text-red-500 hover:text-red-600" title="Eliminar"><i class="fas fa-trash"></i></a>
            </td>
          </tr>
          {% endfor %}
        {% else %}
          <tr>
            <td colspan="5" class="px-6 py-4 text-center text-gray-500">No hay repuestos registrados.</td>
          </tr>
        {% endif %}
      </tbody>
    </table>
  </div>
</div>
{% endblock %}
