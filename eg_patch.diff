*** a/taller/documentos/views/ver_documento_function.py
--- b/taller/documentos/views/ver_documento_function.py
@@
-from .models import Documento
+from decimal import Decimal
+from django.contrib.auth.decorators import login_required
+from django.shortcuts import get_object_or_404, render
+from taller.models.documento import Documento  # ← import correcto
@@
-def ver_documento(request, documento_id):
-    documento = Documento.objects.get(id=documento_id)
-    return render(request, "taller/.../documentos/ver.html", {"documento": documento})
+@login_required
+def ver_documento(request, documento_id):
+    documento = get_object_or_404(
+        Documento.objects.select_related("empresa"),
+        id=documento_id,
+        empresa=request.user.empresa,
+    )
+    # Totales seguros (usa atributos si existen; sino, calcula básico)
+    subtotal_repuestos = getattr(documento, "neto_repuestos", Decimal("0"))
+    subtotal_servicios = getattr(documento, "neto_servicios", Decimal("0"))
+    iva = getattr(documento, "tax_amount", Decimal("0"))
+    total = getattr(documento, "total", subtotal_repuestos + subtotal_servicios + iva)
+    return render(
+        request,
+        "taller/.../documentos/ver.html",
+        {
+            "documento": documento,
+            "subtotal_repuestos": subtotal_repuestos,
+            "subtotal_servicios": subtotal_servicios,
+            "iva": iva,
+            "total": total,
+        },
+    )

*** a/taller/documentos/views/views.py
--- b/taller/documentos/views/views.py
@@
-from django.views.decorators.csrf import csrf_exempt
-from django.views.decorators.http import require_POST
-from django.http import JsonResponse
+from django.views.decorators.http import require_POST
+from django.http import JsonResponse
+from django.contrib.auth.decorators import login_required
 import json
@@
-@csrf_exempt
-def api_crear_tecnico(request):
-    data = json.loads(request.body.decode())
-    nombre = (data.get("nombre") or "").strip()
-    empresa_id = data.get("empresa_id")
-    if not nombre:
-        return JsonResponse({"error": "Nombre requerido"}, status=400)
-    tecnico = Tecnico.objects.create(empresa_id=empresa_id, nombre=nombre, activo=True)
-    return JsonResponse({"ok": True, "id": tecnico.id, "nombre": tecnico.nombre})
+@login_required
+@require_POST
+def api_crear_tecnico(request):
+    try:
+        data = json.loads(request.body.decode() or "{}")
+        nombre = (data.get("nombre") or "").strip()
+        if not nombre:
+            return JsonResponse({"error": "Nombre requerido"}, status=400)
+        tecnico = Tecnico.objects.create(
+            empresa=request.user.empresa,
+            nombre=nombre,
+            activo=True,
+        )
+        return JsonResponse({"ok": True, "id": tecnico.id, "nombre": tecnico.nombre})
+    except Exception as e:
+        return JsonResponse({"error": str(e)}, status=500)

*** a/taller/documentos/forms/documento_form.py
--- b/taller/documentos/forms/documento_form.py
@@
-from dal import autocomplete
-from django import forms
-from taller.models.clientes import Cliente
-from taller.models.documento import Documento
-from taller.models.vehiculos import Vehiculo
+from dal import autocomplete
+from django import forms
+from taller.models.clientes import Cliente
+from taller.models.documento import Documento
+from taller.models.vehiculos import Vehiculo
+
+def _ns(country: str) -> str:
+    """Namespace de autocompletes por país."""
+    return "usa:autocomplete" if (country or "").upper() == "US" else "cl:autocomplete"
@@
 class DocumentoForm(forms.ModelForm):
@@
-    cliente = forms.ModelChoiceField(
+    cliente = forms.ModelChoiceField(
         queryset=Cliente.objects.all(),
         widget=autocomplete.ModelSelect2(
-            url="taller:autocomplete:autocomplete_cliente",
+            url="cl:autocomplete:cliente",  # se sobreescribe en __init__
             attrs={
                 "data-placeholder": "🔍 Buscar cliente...",
                 "data-minimum-input-length": 2,
             },
         ),
         required=False,
     )
@@
-    vehiculo = forms.ModelChoiceField(
+    vehiculo = forms.ModelChoiceField(
         queryset=Vehiculo.objects.all(),
         widget=autocomplete.ModelSelect2(
-            url="taller:autocomplete:vehiculo",
+            url="cl:autocomplete:vehiculo",  # se sobreescribe en __init__
             forward=['cliente'],
             attrs={
                 "data-placeholder": "🔍 Buscar vehículo...",
                 "data-minimum-input-length": 1,
             },
         ),
         required=False,
     )
@@
-        self.country = kwargs.pop('country', 'CL')
+        self.country = (kwargs.pop('country', 'CL') or 'CL').upper()
@@
-        if self.country == "US":
+        if self.country == "US":
             self.fields['tipo'].label = "Document Type"
             self.fields['cliente'].label = "Customer"
             self.fields['vehiculo'].label = "Vehicle"
             self.fields['tecnico_responsable'].label = "Assigned Technician"
             self.fields['fecha_emision'].label = "Issue Date"
             self.fields['kilometraje'].label = "Mileage"
             self.fields['observaciones'].label = "Observations"
             self.fields['pagado'].label = "Paid"
             self.fields['numero'].label = "Number"
@@
-            # Configurar URL de autocompletado para US
-            self.fields['cliente'].widget.url = "usa:autocomplete:cliente"
+            # Configurar URL de autocompletado para US
+            self.fields['cliente'].widget.url = f"{_ns(self.country)}:cliente"
             self.fields['cliente'].widget.attrs["data-placeholder"] = "🔍 Search customer..."
-            self.fields['vehiculo'].widget.url = "usa:autocomplete:vehiculo"
+            self.fields['vehiculo'].widget.url = f"{_ns(self.country)}:vehiculo"
             self.fields['vehiculo'].widget.attrs["data-placeholder"] = "🔍 Search vehicle..."
         else:
             self.fields['tipo'].label = "Tipo de Documento"
             self.fields['cliente'].label = "Cliente"
             self.fields['vehiculo'].label = "Vehículo"
             self.fields['tecnico_responsable'].label = "Técnico Responsable"
             self.fields['fecha_emision'].label = "Fecha de Emisión"
             self.fields['kilometraje'].label = "Kilometraje"
             self.fields['observaciones'].label = "Observations"
             self.fields['pagado'].label = "Pagado"
             self.fields['numero'].label = "Número"
@@
-            # Configurar URL de autocompletado para CL
-            self.fields['cliente'].widget.url = "cl:autocomplete:cliente"
+            # Configurar URL de autocompletado para CL
+            self.fields['cliente'].widget.url = f"{_ns(self.country)}:cliente"
             self.fields['cliente'].widget.attrs["data-placeholder"] = "🔍 Buscar cliente..."
-            self.fields['vehiculo'].widget.url = "cl:autocomplete:vehiculo"
+            self.fields['vehiculo'].widget.url = f"{_ns(self.country)}:vehiculo"
             self.fields['vehiculo'].widget.attrs["data-placeholder"] = "🔍 Buscar vehículo..."
@@
-    class Meta:
-        model = Documento
-        fields = "__all__"
+    class Meta:
+        model = Documento
+        # Mejor explícito para no arrastrar campos futuros sin querer
+        fields = [
+            "tipo",
+            "cliente",
+            "vehiculo",
+            "tecnico_responsable",
+            "fecha_emision",
+            "kilometraje",
+            "observaciones",
+            "pagado",
+            "numero",
+        ]

*** a/taller/documentos/utils/prefix.py
--- /dev/null
+++ b/taller/documentos/utils/prefix.py
@@
+def doc_prefix(tipo: str) -> str:
+    """
+    Prefijo de numeración por tipo de documento.
+    Incluye REC (Recibo/Boleta).
+    """
+    return {
+        "OT": "OT",
+        "FAC": "F",
+        "PRES": "P",
+        "REC": "R",
+    }.get((tipo or "").upper(), "D")

*** a/taller/documentos/views/views_crear.py
--- b/taller/documentos/views/views_crear.py
@@
-import print
+import logging
+log = logging.getLogger(__name__)
@@
-def _template_crear(country):
-    return "taller/cl/es/documentos/crear_documento.html"
+def _template_crear(country: str) -> str:
+    country = (country or "CL").upper()
+    return (
+        "taller/us/en/documentos/crear_documento.html"
+        if country == "US"
+        else "taller/cl/es/documentos/crear_documento.html"
+    )
@@
-    print("Creando documento", request.POST)
+    log.debug("Creando documento %s", request.POST)
@@
-    return redirect("documentos:lista_documentos")
+    return redirect("taller:documentos:lista_documentos")

*** a/taller/documentos/forms/formsets.py
--- b/taller/documentos/forms/formsets.py
@@
-from django.forms import formset_factory
+from django.forms import formset_factory
+from django.core.exceptions import ValidationError
@@
-DetalleRepuestoFormSet = formset_factory(DetalleRepuestoForm, extra=1, can_delete=True)
-DetalleServicioFormSet = formset_factory(DetalleServicioForm, extra=1, can_delete=True)
-DetalleTerceroFormSet  = formset_factory(DetalleTerceroForm,  extra=1, can_delete=True)
+DetalleRepuestoFormSet = formset_factory(DetalleRepuestoForm, extra=0, can_delete=True, min_num=1, validate_min=True)
+DetalleServicioFormSet = formset_factory(DetalleServicioForm, extra=0, can_delete=True)
+DetalleTerceroFormSet  = formset_factory(DetalleTerceroForm,  extra=0, can_delete=True)
@@
-def guardar_formset(formset, doc, empresa, user):
-    if not formset.is_valid():
-        return False
-    for form in formset:
-        if not form.cleaned_data or form.cleaned_data.get("DELETE"):
-            continue
-        obj = form.save(commit=False)
-        obj.documento = doc
-        obj.empresa = empresa
-        obj.created_by = user
-        obj.updated_by = user
-        obj.save()
-    return True
+def guardar_formset(formset, doc, empresa, user):
+    """
+    Guarda líneas amarrándolas al documento/empresa y completando auditoría.
+    Aplica reglas básicas; reglas de IVA/sales tax deben ir en los forms o servicios.
+    """
+    if not formset.is_valid():
+        return False
+    saved_any = False
+    for form in formset:
+        if not getattr(form, "cleaned_data", None) or form.cleaned_data.get("DELETE"):
+            continue
+        obj = form.save(commit=False)
+        # Multi-tenant & herencia básica
+        if hasattr(obj, "documento"):
+            obj.documento = doc
+        if hasattr(obj, "empresa"):
+            obj.empresa = empresa
+        if hasattr(obj, "created_by") and not getattr(obj, "pk", None):
+            obj.created_by = user
+        if hasattr(obj, "updated_by"):
+            obj.updated_by = user
+        obj.save()
+        saved_any = True
+    return saved_any
