===========================================
VERIFICACIÓN: Columna service_id existe
===========================================

RESULTADO:
----------
✓ La columna service_id YA EXISTE en la tabla taller_lineaservicio
  (Línea 9 del PRAGMA: service_id|INTEGER|0||0)

PRÓXIMOS PASOS:
---------------

# 1. Verificar que el sitio funciona ahora
# Abrir: https://www.egarage.cl/us/centro-operaciones-espacial/

# 2. Si todavía falla, puede ser un problema de caché
# Limpiar caché de Python
cd /home/atlantareciclajes/apps/egarage/current
workon venv_egarage310

find . -type d -name "__pycache__" -exec rm -r {} + 2>/dev/null
find . -name "*.pyc" -delete

# 3. Recargar la aplicación en PythonAnywhere
# Dashboard → Web → Reload

# 4. Si sigue fallando, verificar la consulta exacta que está fallando
# Revisar los logs de error en el dashboard

===========================================
VERIFICACIÓN ADICIONAL:
===========================================

# Verificar que el índice también existe
python manage.py dbshell << EOF
SELECT name FROM sqlite_master 
WHERE type='index' 
AND tbl_name='taller_lineaservicio'
AND name LIKE '%service%';
.quit
EOF

# Debe mostrar: taller_lineaservicio_service_id_idx

===========================================
SI EL SITIO AÚN FALLA:
===========================================

# Verificar la consulta exacta que está fallando
# Revisar el traceback completo en el error del navegador
# Puede ser que Django esté usando un alias diferente o haya un problema en el código

===========================================

