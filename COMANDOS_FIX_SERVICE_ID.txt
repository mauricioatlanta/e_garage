===========================================
SOLUCIÓN: Columna service_id faltante
===========================================

PROBLEMA:
---------
OperationalError: no such column: taller_lineaservicio.service_id

SOLUCIÓN (Ejecutar en el servidor):
-----------------------------------

cd /home/atlantareciclajes/apps/egarage/current
workon venv_egarage310

# OPCIÓN 1: Agregar columna manualmente (RÁPIDA)
# -----------------------------------------------

python manage.py dbshell << 'SQL'
ALTER TABLE taller_lineaservicio 
ADD COLUMN service_id INTEGER NULL;
CREATE INDEX IF NOT EXISTS taller_lineaservicio_service_id_idx 
ON taller_lineaservicio(service_id);
.quit
SQL

# Verificar
python manage.py dbshell << EOF
.schema taller_lineaservicio | grep service
.quit
EOF

# Probar el sitio
# https://www.egarage.cl/us/centro-operaciones-espacial/


# OPCIÓN 2: Crear y aplicar migración (RECOMENDADA)
# --------------------------------------------------

# Verificar si hay migraciones pendientes
python manage.py showmigrations taller | grep "\[ \]"

# Crear migración para el campo faltante
python manage.py makemigrations taller

# Aplicar
python manage.py migrate taller

# Verificar
python manage.py check


# OPCIÓN 3: Si la migración 0028 no se aplicó correctamente
# ----------------------------------------------------------

# Verificar si 0028 está aplicada
python manage.py showmigrations taller | grep 0028

# Si no está aplicada, aplicarla
python manage.py migrate taller 0028

# Si falla porque ya existe algo, marcarla como fake
python manage.py migrate taller 0028 --fake

===========================================
VERIFICACIÓN:
===========================================

# Ver estructura de la tabla
python manage.py dbshell << EOF
.schema taller_lineaservicio
.quit
EOF

# Debe mostrar service_id en la lista de columnas

===========================================

