{% extends 'base.html' %}
{% load widget_tweaks %}

{% block content %}
<style>
body {
  background: linear-gradient(135deg, #050505 0%, #111111 25%, #1a1a1a 50%, #111111 75%, #050505 100%);
  min-height: 100vh;
  overflow-x: hidden;
  position: relative;
}

/* Fondo ultra moderno con destellos más sutiles */
.futuristic-bg {
  position: fixed;
  top: 0; left: 0; width: 100vw; height: 100vh;
  z-index: -1;
  background:
    radial-gradient(circle at 20% 30%, rgba(80, 80, 80, 0.1) 0%, transparent 50%),
    radial-gradient(circle at 80% 70%, rgba(50, 50, 50, 0.15) 0%, transparent 50%),
    radial-gradient(circle at 40% 80%, rgba(70, 70, 70, 0.08) 0%, transparent 40%),
    linear-gradient(135deg, #000000 0%, #0f0f0f 25%, #1e1e1e 50%, #0f0f0f 75%, #000000 100%);
  animation: shimmer 8s ease-in-out infinite alternate;
}

/* Partículas flotantes más sutiles */
.futuristic-bg::before {
  content: '';
  position: absolute;
  top: 0; left: 0; width: 100%; height: 100%;
  background-image:
    radial-gradient(1px 1px at 20px 30px, rgba(200, 200, 200, 0.4), transparent),
    radial-gradient(1px 1px at 40px 70px, rgba(150, 150, 150, 0.3), transparent),
    radial-gradient(0.5px 0.5px at 90px 40px, rgba(180, 180, 180, 0.3), transparent),
    radial-gradient(0.5px 0.5px at 130px 80px, rgba(160, 160, 160, 0.25), transparent),
    radial-gradient(1px 1px at 160px 30px, rgba(140, 140, 140, 0.3), transparent);
  background-repeat: repeat;
  background-size: 250px 250px;
  animation: sparkle 12s linear infinite;
}

/* Efecto de destello dinámico */
.futuristic-bg::after {
  content: '';
  position: absolute;
  top: -50%; left: -50%;
  width: 200%; height: 200%;
  background:
    radial-gradient(circle, transparent 20%, rgba(255, 255, 255, 0.05) 21%, rgba(255, 255, 255, 0.05) 34%, transparent 35%),
    linear-gradient(45deg, transparent 30%, rgba(128, 128, 128, 0.1) 50%, transparent 70%);
  animation: flash 4s ease-in-out infinite;
}

@keyframes shimmer {
  0% { filter: brightness(0.9) contrast(1.1); }
  50% { filter: brightness(1.1) contrast(1.3); }
  100% { filter: brightness(0.95) contrast(1.15); }
}

@keyframes sparkle {
  0% { transform: translateY(0px) rotate(0deg); opacity: 1; }
  50% { transform: translateY(-20px) rotate(180deg); opacity: 0.8; }
  100% { transform: translateY(0px) rotate(360deg); opacity: 1; }
}

@keyframes flash {
  0%, 100% { opacity: 0; transform: rotate(0deg); }
  25% { opacity: 0.3; transform: rotate(90deg); }
  50% { opacity: 0.6; transform: rotate(180deg); }
  75% { opacity: 0.4; transform: rotate(270deg); }
}

.futuristic-form {
  background: rgba(8, 8, 8, 0.97);
  border-radius: 2rem;
  box-shadow:
    0 0 30px 8px rgba(80, 80, 80, 0.2),
    0 0 0 1px rgba(60, 60, 60, 0.6),
    inset 0 0 25px rgba(40, 40, 40, 0.15);
  padding: 2.5rem;
  margin-top: 3rem;
  color: #d8d8d8;
  font-family: 'Orbitron', 'Share Tech Mono', 'Segoe UI', Arial, sans-serif;
  backdrop-filter: blur(15px);
}

.futuristic-label {
  color: #a8a8a8;
  font-weight: bold;
  letter-spacing: 1.5px;
  text-shadow: 0 0 6px rgba(168, 168, 168, 0.4);
  margin-bottom: 0.5rem;
  display: block;
}

input.futuristic-input,
select.futuristic-input,
textarea.futuristic-input {
  background: #0d0d0d !important;
  color: #c8c8c8 !important;
  border: 1.5px solid #484848 !important;
  border-radius: 0.7rem !important;
  font-family: 'Share Tech Mono', 'Orbitron', monospace !important;
  font-size: 1.1rem !important;
  box-shadow:
    0 0 8px rgba(80, 80, 80, 0.3),
    inset 0 0 8px rgba(32, 32, 32, 0.4);
  padding: 0.8rem !important;
  width: 100% !important;
  transition: all 0.3s ease;
}

input.futuristic-input:focus,
select.futuristic-input:focus,
textarea.futuristic-input:focus {
  border-color: #707070 !important;
  box-shadow:
    0 0 15px rgba(112, 112, 112, 0.4),
    inset 0 0 12px rgba(60, 60, 60, 0.3) !important;
  transform: scale(1.01);
}

input.futuristic-input::placeholder,
textarea.futuristic-input::placeholder {
  color: rgba(128, 128, 128, 0.7) !important;
}

.futuristic-btn {
  background: linear-gradient(135deg, #2a2a2a 0%, #404040 50%, #565656 100%);
  color: #e8e8e8;
  font-family: 'Orbitron', 'Share Tech Mono', Arial, sans-serif;
  font-size: 1.2rem;
  font-weight: bold;
  border-radius: 1rem;
  box-shadow:
    0 0 15px rgba(86, 86, 86, 0.3),
    inset 0 0 8px rgba(110, 110, 110, 0.15);
  padding: 0.8rem 2.5rem;
  transition: all 0.3s ease;
  border: none;
  cursor: pointer;
  text-shadow: 0 0 4px rgba(232, 232, 232, 0.3);
}

.futuristic-btn:hover {
  transform: scale(1.08);
  background: linear-gradient(135deg, #505050 0%, #707070 50%, #909090 100%);
  box-shadow:
    0 0 40px rgba(144, 144, 144, 0.6),
    inset 0 0 15px rgba(192, 192, 192, 0.3);
}

.futuristic-btn-secondary {
  background: linear-gradient(135deg, #2a2a2a 0%, #3a3a3a 50%, #4a4a4a 100%);
  color: #e0e0e0;
  font-family: 'Orbitron', 'Share Tech Mono', Arial, sans-serif;
  font-size: 1.2rem;
  font-weight: bold;
  border-radius: 1rem;
  box-shadow:
    0 0 20px rgba(74, 74, 74, 0.4),
    inset 0 0 10px rgba(96, 96, 96, 0.2);
  padding: 0.8rem 2.5rem;
  transition: all 0.3s ease;
  border: none;
  cursor: pointer;
  text-shadow: 0 0 5px rgba(224, 224, 224, 0.5);
}

.futuristic-btn-secondary:hover {
  transform: scale(1.08);
  background: linear-gradient(135deg, #3a3a3a 0%, #4a4a4a 50%, #5a5a5a 100%);
  box-shadow:
    0 0 40px rgba(90, 90, 90, 0.6),
    inset 0 0 15px rgba(128, 128, 128, 0.3);
}

.futuristic-title {
  font-family: 'Orbitron', 'Share Tech Mono', Arial, sans-serif;
  font-size: 3rem;
  color: #b8b8b8;
  text-align: center;
  margin-bottom: 2rem;
  text-shadow:
    0 0 15px rgba(184, 184, 184, 0.4),
    0 0 1px #ddd,
    0 0 30px rgba(100, 100, 100, 0.3);
  letter-spacing: 0.15em;
}

.config-section {
  background: rgba(12, 12, 12, 0.9);
  border-radius: 1rem;
  padding: 1.5rem;
  margin-bottom: 1.5rem;
  border: 1px solid rgba(80, 80, 80, 0.25);
  box-shadow:
    0 0 12px rgba(60, 60, 60, 0.15),
    inset 0 0 20px rgba(32, 32, 32, 0.12);
  backdrop-filter: blur(8px);
}

.personal-section {
  background: rgba(18, 18, 18, 0.9);
  border-radius: 1rem;
  padding: 1.5rem;
  margin-bottom: 1.5rem;
  border: 1px solid rgba(70, 70, 70, 0.25);
  box-shadow:
    0 0 12px rgba(50, 50, 50, 0.15),
    inset 0 0 20px rgba(28, 28, 28, 0.12);
  backdrop-filter: blur(8px);
}

.info-box {
  background: rgba(20, 20, 20, 0.8);
  border-left: 3px solid #606060;
  padding: 1rem;
  margin-bottom: 1rem;
  border-radius: 0.5rem;
  box-shadow:
    0 0 8px rgba(96, 96, 96, 0.15),
    inset 0 0 12px rgba(40, 40, 40, 0.1);
}
</style>
<link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@700&family=Share+Tech+Mono&display=swap" rel="stylesheet">
<div class="futuristic-bg"></div>
<div class="max-w-6xl mx-auto futuristic-form">
  <h1 class="futuristic-title">⚙️ Configuración del Taller</h1>

  {% if created %}
  <div class="info-box">
    <h3 class="text-lg font-bold mb-2" style="color: #c0c0c0;">🆕 Bienvenido a TallerPro</h3>
    <p style="color: #a0a0a0;">Esta es tu primera vez configurando el taller. Completa los datos para personalizar tu experiencia.</p>
  </div>
  {% endif %}

  <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
    <!-- Formulario de Datos Personales -->
    <div class="personal-section">
      <h3 class="futuristic-label text-xl mb-4" style="color: #a0a0a0;">👤 Datos Personales</h3>
      <form method="post" class="space-y-4">
        {% csrf_token %}
        <input type="hidden" name="datos_form" value="1">

        <div>
          <label class="futuristic-label" style="color: #b0b0b0;" for="id_first_name">{{ datos_form.first_name.label }}:</label>
          {{ datos_form.first_name|add_class:'futuristic-input' }}
        </div>

        <div>
          <label class="futuristic-label" style="color: #b0b0b0;" for="id_last_name">{{ datos_form.last_name.label }}:</label>
          {{ datos_form.last_name|add_class:'futuristic-input' }}
        </div>

        <div>
          <label class="futuristic-label" style="color: #b0b0b0;" for="id_email">{{ datos_form.email.label }}:</label>
          {{ datos_form.email|add_class:'futuristic-input' }}
        </div>

        <div class="text-center pt-4">
          <button class="futuristic-btn-secondary" type="submit">
            💾 Guardar Datos Personales
          </button>
        </div>
      </form>
    </div>

    <!-- Formulario de Empresa -->
    <div class="config-section">
      <h3 class="futuristic-label text-xl mb-4">🏢 Datos de la Empresa</h3>
      <form method="post" enctype="multipart/form-data" class="space-y-4">
        {% csrf_token %}
        <input type="hidden" name="empresa_form" value="1">

        <div>
          <label class="futuristic-label" for="id_nombre_taller">{{ empresa_form.nombre_taller.label }}:</label>
          {{ empresa_form.nombre_taller|add_class:'futuristic-input' }}
        </div>

        <div>
          <label class="futuristic-label" for="id_empresa">{{ empresa_form.empresa.label }}:</label>
          {{ empresa_form.empresa|add_class:'futuristic-input' }}
        </div>

        <div>
          <label class="futuristic-label" for="id_direccion">{{ empresa_form.direccion.label }}:</label>
          {{ empresa_form.direccion|add_class:'futuristic-input' }}
        </div>

        <div>
          <label class="futuristic-label" for="id_telefono">{{ empresa_form.telefono.label }}:</label>
          {{ empresa_form.telefono|add_class:'futuristic-input' }}
        </div>

        <div>
          <label class="futuristic-label" for="id_logo">{{ empresa_form.logo.label }}:</label>
          {{ empresa_form.logo|add_class:'futuristic-input' }}

          <!-- Vista previa simple y robusta -->
          <div id="preview-section" class="mt-4">
            <!-- Mensaje inicial -->
            <div id="no-file-message" class="text-gray-400 text-sm">
              📷 Selecciona una imagen para ver la vista previa
            </div>

            <!-- Vista previa de imagen -->
            <div id="preview-container" class="hidden">
              <p class="text-green-400 mb-2 font-bold">✅ Imagen seleccionada:</p>
              <div class="bg-gray-800 p-4 rounded-lg border border-green-400">
                <img id="preview-image" src="" alt="Vista previa" class="max-w-full max-h-64 h-auto mb-4 rounded">
                <div class="text-sm text-gray-300">
                  <p><strong>📁 Archivo:</strong> <span id="preview-name"></span></p>
                  <p><strong>📏 Tamaño:</strong> <span id="preview-size"></span></p>
                  <p><strong>🔍 Tipo:</strong> <span id="preview-type"></span></p>
                </div>
              </div>
            </div>

            <!-- Mensaje de error -->
            <div id="error-message" class="hidden bg-red-900 border border-red-600 p-3 rounded text-red-200 text-sm">
            </div>
          </div>

          {% if empresa.logo %}
          <div class="mt-4">
            <p class="text-cyan-300 mb-2">Logo actual:</p>
            <img src="{{ empresa.logo.url }}" alt="Logo actual" class="max-w-xs h-auto border-2 border-cyan-400 rounded-lg">
          </div>
          {% endif %}
        </div>

        <div class="text-center pt-4">
          <button class="futuristic-btn" type="submit">
            💾 Guardar Datos de la Empresa
          </button>
        </div>
      </form>
    </div>
  </div>

  <!-- Estado de la Cuenta -->
  <div class="config-section mt-8">
    <h3 class="futuristic-label text-xl mb-4">📊 Estado de la Cuenta</h3>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 text-center">
      <div class="bg-gradient-to-r from-green-500 to-green-600 p-4 rounded-lg">
        <div class="text-2xl font-bold">{{ empresa.dias_restantes }}</div>
        <div class="text-sm">Días restantes</div>
      </div>
      <div class="bg-gradient-to-r from-blue-500 to-blue-600 p-4 rounded-lg">
        <div class="text-2xl font-bold">{{ empresa.suscripcion_activa|yesno:"✅,❌" }}</div>
        <div class="text-sm">Suscripción</div>
      </div>
      <div class="bg-gradient-to-r from-purple-500 to-purple-600 p-4 rounded-lg">
        <div class="text-2xl font-bold">{{ empresa.fecha_inicio|date:"d/m/Y" }}</div>
        <div class="text-sm">Fecha de inicio</div>
      </div>
    </div>
  </div>

  <!-- Botón volver -->
  <div class="text-center pt-6">
    <a href="{% url 'dashboard' %}" class="futuristic-btn" style="background: linear-gradient(90deg, #666 0%, #999 100%);">
      🔙 Volver al Dashboard
    </a>
  </div>
</div>

<script>
// Vista previa y validación de archivos para logo
document.addEventListener('DOMContentLoaded', function() {
    const logoInput = document.getElementById('id_logo');
    const previewContainer = document.getElementById('image-preview-container');
    const imagePreview = document.getElementById('image-preview');
    const fileName = document.getElementById('file-name');
    const fileSize = document.getElementById('file-size');
    const empresaForm = document.querySelector('form[method="post"]');

    if (logoInput) {
        logoInput.addEventListener('change', function(e) {
            const file = e.target.files[0];

            console.log('🔄 Event change disparado');
            console.log('📂 Files length:', e.target.files.length);
            console.log('📂 Input value:', e.target.value);

            if (file) {
                console.log('🖼️ Archivo seleccionado:', file);
                console.log('📁 Nombre completo:', file.name);
                console.log('� Ruta input:', e.target.value);
                console.log('�📏 Tamaño:', file.size, 'bytes');
                console.log('🔍 Tipo MIME:', file.type);
                console.log('📅 Última modificación:', new Date(file.lastModified));

                // Debug adicional del archivo
                if (file.webkitRelativePath) {
                    console.log('📁 Ruta relativa:', file.webkitRelativePath);
                }

                // Validaciones
                if (file.size === 0) {
                    alert('❌ El archivo está vacío. Selecciona un archivo válido.');
                    logoInput.value = '';
                    previewContainer.classList.add('hidden');
                    return;
                }

                if (file.size > 5 * 1024 * 1024) { // 5MB
                    alert('❌ El archivo es muy grande. Máximo 5MB.');
                    logoInput.value = '';
                    previewContainer.classList.add('hidden');
                    return;
                }

                if (!file.type.startsWith('image/')) {
                    alert('❌ Solo se permiten archivos de imagen.');
                    logoInput.value = '';
                    previewContainer.classList.add('hidden');
                    return;
                }

                // Mostrar información del archivo
                fileName.textContent = file.name;
                fileSize.textContent = (file.size / 1024).toFixed(1) + ' KB';

                // Crear vista previa de la imagen
                const reader = new FileReader();
                reader.onload = function(e) {
                    imagePreview.src = e.target.result;
                    previewContainer.classList.remove('hidden');

                    // Agregar animación de aparición
                    previewContainer.style.opacity = '0';
                    previewContainer.style.transform = 'translateY(20px)';
                    previewContainer.style.transition = 'all 0.3s ease';

                    setTimeout(() => {
                        previewContainer.style.opacity = '1';
                        previewContainer.style.transform = 'translateY(0)';
                    }, 50);

                    console.log('✅ Vista previa cargada correctamente');
                };

                reader.onerror = function() {
                    alert('❌ Error al cargar la vista previa de la imagen.');
                    previewContainer.classList.add('hidden');
                };

                reader.readAsDataURL(file);
                console.log('✅ Archivo válido y vista previa en proceso...');

            } else {
                console.log('❌ No se seleccionó archivo');
                previewContainer.classList.add('hidden');
            }
        });

        // Efecto visual al hacer hover sobre el input de archivo
        logoInput.addEventListener('mouseover', function() {
            this.style.borderColor = '#00ff88';
            this.style.boxShadow = '0 0 10px rgba(0, 255, 136, 0.3)';
        });

        logoInput.addEventListener('mouseout', function() {
            this.style.borderColor = '';
            this.style.boxShadow = '';
        });
    }

    // Debug del envío del formulario
    if (empresaForm) {
        empresaForm.addEventListener('submit', function(e) {
            console.log('\n📤 ENVIANDO FORMULARIO:');
            console.log('🔍 Form data:');

            const formData = new FormData(empresaForm);
            for (let [key, value] of formData.entries()) {
                if (value instanceof File) {
                    console.log(`  ${key}: [FILE] ${value.name} (${value.size} bytes, ${value.type})`);
                } else {
                    console.log(`  ${key}: ${value}`);
                }
            }

            // Verificar específicamente el logo
            const logoFile = formData.get('logo');
            if (logoFile && logoFile.size > 0) {
                console.log('✅ Logo incluido en FormData');
                console.log(`  📁 Nombre: ${logoFile.name}`);
                console.log(`  📏 Tamaño: ${logoFile.size} bytes`);
            } else {
                console.log('❌ Logo NO incluido o vacío en FormData');
            }

            console.log('📤 Formulario enviado...\n');
        });
    }
});
</script>

{% endblock content %}
