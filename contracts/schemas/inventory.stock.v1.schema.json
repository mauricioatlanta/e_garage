{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "egarage/contracts/schemas/inventory.stock.v1.schema.json",
  "title": "Stock Contract — Payload Schema (SUPERSEDED)",
  "description": "SUPERSEDED. This schema was split into two schemas when inventory.stock.v1 was migrated to meta-contract v2 format and the domain boundary with inventory.reservation.v1 was established (ADR-008). Use: inventory.stock.updated.schema.json, inventory.stock.snapshot.schema.json. Kept for historical reference only.",
  "type": "object",
  "required": ["updates", "batch_sequence", "timestamp"],
  "additionalProperties": false,
  "properties": {
    "updates": {
      "type": "array",
      "minItems": 1,
      "description": "One or more stock changes in this push. Each item is a delta for a single product.",
      "items": {
        "type": "object",
        "required": ["product_id", "quantity_available", "sequence"],
        "additionalProperties": false,
        "properties": {
          "product_id": {
            "type": "string",
            "description": "References the id field in catalog.product-knowledge.v1"
          },
          "quantity_available": {
            "type": "integer",
            "minimum": 0,
            "description": "Current available quantity after this update. Not a delta — always an absolute value."
          },
          "sequence": {
            "type": "integer",
            "minimum": 1,
            "description": "Monotonically increasing per product_id. Consumers must discard any message where sequence <= last seen sequence for this product_id."
          },
          "warehouse_id": {
            "type": ["string", "null"],
            "description": "Null until ADR-004 is resolved. Multi-warehouse support pending."
          }
        }
      }
    },
    "batch_sequence": {
      "type": "integer",
      "minimum": 1,
      "description": "Monotonically increasing batch identifier. Consumers can use this to detect dropped batches."
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of when erp.inventory generated this push"
    }
  }
}
