COMANDO PARA EJECUTAR EN EL SERVIDOR:

# Restaurar el archivo completo desde Git (recomendado)
cd /home/atlantareciclajes/apps/egarage/current && git checkout HEAD -- taller/documentos/views.py && python3 -m py_compile taller/documentos/views.py && touch /var/www/www_egarage_cl_wsgi.py && echo "✅ Archivo restaurado y servidor reiniciado"

# O si prefieres hacer pull primero:
cd /home/atlantareciclajes/apps/egarage/current && git pull origin main --no-rebase && git checkout HEAD -- taller/documentos/views.py && python3 -m py_compile taller/documentos/views.py && touch /var/www/www_egarage_cl_wsgi.py && echo "✅ Archivo actualizado y servidor reiniciado"

