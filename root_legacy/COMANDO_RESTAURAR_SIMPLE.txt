═══════════════════════════════════════════════════════════════
COMANDO SIMPLE: RESTAURAR DESDE GIT
═══════════════════════════════════════════════════════════════

Ejecuta este comando para restaurar el archivo completamente:

cd /home/atlantareciclajes/apps/egarage/current && \
cp gestion_taller/urls.py gestion_taller/urls.py.backup_final_$(date +%Y%m%d_%H%M%S) && \
git checkout HEAD -- gestion_taller/urls.py && \
python3 -c "import ast; ast.parse(open('gestion_taller/urls.py', 'r', encoding='utf-8').read()); print('✅ Archivo restaurado y sintaxis correcta')" && \
touch /var/www/www_egarage_cl_wsgi.py && \
echo "✅ Servidor reiniciado"

═══════════════════════════════════════════════════════════════
