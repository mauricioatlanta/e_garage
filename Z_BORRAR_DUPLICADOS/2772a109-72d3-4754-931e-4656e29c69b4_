{% extends "base.html" %}
{% load i18n static widget_tweaks %}

{% block content %}
<div class="container-fluid">
  <div class="row">
    <div class="col-12">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0">{% trans "Vehicles" %}</h1>
        <a href="/us/vehiculos/crear/" class="btn btn-primary">
          <i class="fas fa-plus"></i> {% trans "Add Vehicle" %}
        </a>
      </div>

      {% if vehiculos %}
        <div class="row">
          {% for vehiculo in vehiculos %}
          <div class="col-md-6 col-lg-4 mb-4">
            <div class="card">
              <div class="card-body">
                <h5 class="card-title">{{ vehiculo.marca.nombre }} {{ vehiculo.modelo.nombre }}</h5>
                <p class="card-text">
                  <strong>{% trans "License Plate" %}:</strong> {{ vehiculo.patente }}<br>
                  <strong>{% trans "VIN" %}:</strong> {{ vehiculo.vin }}<br>
                  <strong>{% trans "Year" %}:</strong> {{ vehiculo.anio }}<br>
                  <strong>{% trans "Client" %}:</strong> {{ vehiculo.cliente.nombre }}
                </p>
                <div class="btn-group" role="group">
                  <a href="/us/vehiculos/{{ vehiculo.pk }}/" class="btn btn-sm btn-outline-primary">
                    <i class="fas fa-eye"></i> {% trans "View" %}
                  </a>
                  <a href="/us/vehiculos/{{ vehiculo.pk }}/editar/" class="btn btn-sm btn-outline-secondary">
                    <i class="fas fa-edit"></i> {% trans "Edit" %}
                  </a>
                </div>
              </div>
            </div>
          </div>
          {% endfor %}
        </div>
      {% else %}
        <div class="text-center py-5">
          <i class="fas fa-car fa-3x text-muted mb-3"></i>
          <h4>{% trans "No vehicles found" %}</h4>
          <p class="text-muted">{% trans "Start by adding your first vehicle." %}</p>
          <a href="/us/vehiculos/crear/" class="btn btn-primary">
            <i class="fas fa-plus"></i> {% trans "Add Vehicle" %}
          </a>
        </div>
      {% endif %}
    </div>
  </div>
</div>
{% endblock %}
