{% extends "base.html" %}
{% load static %}

{% block title %}Dashboard de Inteligencia de Negocios{% endblock %}

{% block content %}
<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    
    <!-- Header con opción de refrescar -->
    <div class="flex justify-between items-center mb-8">
        <div>
            <h1 class="text-3xl font-bold text-gray-900">Dashboard General</h1>
            <p class="text-sm text-gray-500 mt-1">Indicadores clave de rendimiento del mes actual</p>
        </div>
        <a href="{{ refresh_url }}" class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50">
            <svg class="h-4 w-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
            </svg>
            Actualizar
        </a>
    </div>

    <!-- 1. Tarjetas de KPIs -->
    <div class="grid grid-cols-1 gap-5 sm:grid-cols-2 lg:grid-cols-4 mb-8">
        <!-- Ventas del Mes -->
        <div class="bg-white overflow-hidden shadow rounded-lg hover:shadow-lg transition-shadow">
            <div class="p-5">
                <div class="flex items-center">
                    <div class="flex-shrink-0 bg-indigo-500 rounded-md p-3">
                        <!-- Icono Dinero -->
                        <svg class="h-6 w-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                    </div>
                    <div class="ml-5 w-0 flex-1">
                        <dl>
                            <dt class="text-sm font-medium text-gray-500 truncate">Ventas este Mes</dt>
                            <dd class="text-lg font-medium text-gray-900">
                                {{ moneda }} {{ kpis.total_ventas|floatformat:0|default:"0" }}
                            </dd>
                        </dl>
                    </div>
                </div>
            </div>
        </div>

        <!-- OTs Activas -->
        <div class="bg-white overflow-hidden shadow rounded-lg hover:shadow-lg transition-shadow">
            <div class="p-5">
                <div class="flex items-center">
                    <div class="flex-shrink-0 bg-green-500 rounded-md p-3">
                        <svg class="h-6 w-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"></path>
                        </svg>
                    </div>
                    <div class="ml-5 w-0 flex-1">
                        <dl>
                            <dt class="text-sm font-medium text-gray-500 truncate">Órdenes Realizadas</dt>
                            <dd class="text-lg font-medium text-gray-900">
                                {{ kpis.total_ot|default:"0" }}
                            </dd>
                        </dl>
                    </div>
                </div>
            </div>
        </div>

        <!-- Facturas -->
        <div class="bg-white overflow-hidden shadow rounded-lg hover:shadow-lg transition-shadow">
            <div class="p-5">
                <div class="flex items-center">
                    <div class="flex-shrink-0 bg-blue-500 rounded-md p-3">
                        <svg class="h-6 w-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                        </svg>
                    </div>
                    <div class="ml-5 w-0 flex-1">
                        <dl>
                            <dt class="text-sm font-medium text-gray-500 truncate">Facturas Emitidas</dt>
                            <dd class="text-lg font-medium text-gray-900">
                                {{ kpis.total_facturas|default:"0" }}
                            </dd>
                        </dl>
                    </div>
                </div>
            </div>
        </div>

        <!-- Ticket Promedio -->
        <div class="bg-white overflow-hidden shadow rounded-lg hover:shadow-lg transition-shadow">
            <div class="p-5">
                <div class="flex items-center">
                    <div class="flex-shrink-0 bg-yellow-500 rounded-md p-3">
                        <svg class="h-6 w-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
                        </svg>
                    </div>
                    <div class="ml-5 w-0 flex-1">
                        <dl>
                            <dt class="text-sm font-medium text-gray-500 truncate">Ticket Promedio</dt>
                            <dd class="text-lg font-medium text-gray-900">
                                {{ moneda }} {{ kpis.ticket_promedio|floatformat:0|default:"0" }}
                            </dd>
                        </dl>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- 2. Gráfico y Tablas -->
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
        
        <!-- Gráfico de Ventas -->
        <div class="bg-white shadow rounded-lg p-6 lg:col-span-2">
            <div class="flex justify-between items-center mb-4">
                <h3 class="text-lg font-medium leading-6 text-gray-900">Tendencia de Ventas (12 Meses)</h3>
                <div class="text-xs text-gray-500">Últimos 12 meses</div>
            </div>
            <div class="relative h-80">
                <canvas id="salesChart"></canvas>
            </div>
        </div>

        <!-- Ranking Técnicos -->
        <div class="bg-white shadow rounded-lg p-6">
            <h3 class="text-lg font-medium leading-6 text-gray-900 mb-4">Top Técnicos (Este Mes)</h3>
            <ul class="divide-y divide-gray-200">
                {% for tecnico in top_tecnicos %}
                <li class="py-4 flex items-center justify-between">
                    <div class="flex items-center">
                        <span class="h-8 w-8 rounded-full bg-indigo-100 flex items-center justify-center text-sm font-bold text-indigo-600">
                            {{ forloop.counter }}
                        </span>
                        <div class="ml-3">
                            <p class="text-sm font-medium text-gray-900">{{ tecnico.nombre }}</p>
                            <p class="text-xs text-gray-500">{{ tecnico.total_trabajos }} trabajos</p>
                        </div>
                    </div>
                    <div class="text-sm font-semibold text-indigo-600">
                        {{ moneda }} {{ tecnico.total_generado|floatformat:0|default:"0" }}
                    </div>
                </li>
                {% empty %}
                <li class="py-4 text-sm text-gray-500 italic text-center">
                    No hay datos registrados este mes.
                </li>
                {% endfor %}
            </ul>
        </div>
    </div>

    <!-- 3. KPIs Adicionales (Opcional) -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-5 mt-8">
        <!-- Presupuestos -->
        <div class="bg-white shadow rounded-lg p-6">
            <h3 class="text-lg font-medium text-gray-900 mb-4">Presupuestos</h3>
            <div class="text-3xl font-bold text-gray-900">
                {{ kpis.total_presupuestos|default:"0" }}
            </div>
            <p class="text-sm text-gray-500 mt-1">Emitidos este mes</p>
        </div>

        <!-- Tasa de Conversión -->
        <div class="bg-white shadow rounded-lg p-6">
            <h3 class="text-lg font-medium text-gray-900 mb-4">Tasa de Conversión</h3>
            <div class="text-3xl font-bold text-gray-900">
                {{ kpis.tasa_conversion|default:"0"|floatformat:1 }}%
            </div>
            <p class="text-sm text-gray-500 mt-1">Presupuestos → Facturas</p>
        </div>
    </div>

</div>

<!-- Script para Gráfico (Chart.js) -->
<script src="https://cdn.jsdelivr.net/npm/chart.js@3.9.1/dist/chart.min.js"></script>
<script>
    // Configurar gráfico de ventas
    const ctx = document.getElementById('salesChart').getContext('2d');
    
    // Datos del contexto Django
    const chartLabels = {{ chart_labels|safe }};
    const chartValues = {{ chart_values|safe }};
    
    // Crear gráfico
    const salesChart = new Chart(ctx, {
        type: 'line', // o 'bar' para barras
        data: {
            labels: chartLabels,
            datasets: [{
                label: 'Ventas Totales',
                data: chartValues,
                backgroundColor: 'rgba(79, 70, 229, 0.1)',
                borderColor: 'rgba(79, 70, 229, 1)',
                borderWidth: 2,
                tension: 0.4, // Curva suave
                fill: true,
                pointBackgroundColor: 'rgba(79, 70, 229, 1)',
                pointBorderColor: '#fff',
                pointBorderWidth: 2,
                pointRadius: 4,
                pointHoverRadius: 6,
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            plugins: {
                legend: {
                    display: false
                },
                tooltip: {
                    backgroundColor: 'rgba(0, 0, 0, 0.8)',
                    titleColor: '#fff',
                    bodyColor: '#fff',
                    borderColor: 'rgba(79, 70, 229, 1)',
                    borderWidth: 1,
                    padding: 12,
                    displayColors: false,
                    callbacks: {
                        label: function(context) {
                            return '{{ moneda }}' + context.parsed.y.toLocaleString();
                        }
                    }
                }
            },
            scales: {
                y: {
                    beginAtZero: true,
                    ticks: {
                        callback: function(value) {
                            return '{{ moneda }}' + value.toLocaleString();
                        }
                    },
                    grid: {
                        color: 'rgba(0, 0, 0, 0.05)'
                    }
                },
                x: {
                    grid: {
                        display: false
                    }
                }
            }
        }
    });
</script>
{% endblock %}

