{% extends "base.html" %}
{% load i18n static %}
{% load country_url %}

{% block title %}{% trans "Password Reset Email Sent" %} - eGarage{% endblock %}

{% block extra_head %}
  <style>
    /* ——— Estilos optimizados para producción ——— */
    body.auth-page {
      min-height: 100vh;
      margin: 0;
      background: linear-gradient(135deg, #0f0f23 0%, #1a1a2e 25%, #16213e 50%, #0f3460 75%, #533483 100%);
      background-size: 400% 400%;
      animation: gradientShift 15s ease infinite;
      overflow-x: hidden;
    }

    @keyframes gradientShift {
      0%{background-position:0% 50%}
      50%{background-position:100% 50%}
      100%{background-position:0% 50%}
    }

    .egarage-logo{
      text-align:center;
      padding-top:2rem;
      margin-bottom:2rem
    }

    .egarage-logo h1{
      color:#00eaff;
      font-family:'Orbitron','Segoe UI',Arial,sans-serif;
      font-size:3rem;
      font-weight:900;
      margin:0;
      text-shadow:0 0 30px #00eaff88;
      letter-spacing:3px
    }

    .egarage-logo .tagline{
      color:#7fdcff;
      font-size:1rem;
      margin-top:0.5rem;
      opacity:.8
    }

    .reset-done-container{
      max-width:520px;
      margin:2rem auto;
      background:rgba(15,15,35,.95);
      border-radius:24px;
      border:2px solid rgba(0,234,255,.3);
      padding:3rem 2.5rem;
      color:#fff;
      text-align:center;
      position:relative;
      overflow:hidden
    }

    .reset-done-container h2{
      color:#00eaff;
      font-family:'Orbitron','Segoe UI',Arial,sans-serif;
      font-size:2rem;
      font-weight:700;
      margin-bottom:1rem
    }

    .reset-done-container p{
      color:#b2e0ff;
      margin-bottom:1.5rem;
      font-size:1.05rem;
      line-height:1.6
    }

    .btn-principal{
      background: linear-gradient(135deg, #00eaff 0%, #a3ff12 50%, #ff00ea 100%);
      color:#000;
      font-family:'Orbitron','Segoe UI',Arial,sans-serif;
      font-weight:700;
      padding:1rem 1.25rem;
      border:none;
      border-radius:12px;
      display:inline-block;
      text-decoration:none;
      width:100%;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .btn-principal:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(0, 234, 255, 0.3);
    }

    .additional-info {
      background: rgba(0, 234, 255, 0.1);
      border: 1px solid rgba(0, 234, 255, 0.2);
      border-radius: 12px;
      padding: 1.5rem;
      margin: 2rem 0;
      text-align: left;
    }

    .additional-info h3 {
      color: #00eaff;
      font-size: 1.2rem;
      margin-bottom: 1rem;
      text-align: center;
    }

    .additional-info ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    .additional-info li {
      color: #b2e0ff;
      margin-bottom: 0.8rem;
      padding-left: 1.5rem;
      position: relative;
    }

    .additional-info li:before {
      content: "✓";
      color: #a3ff12;
      font-weight: bold;
      position: absolute;
      left: 0;
    }

    .footer{
      text-align:center;
      margin-top:2rem;
      color:#7fdcff;
      font-size:.9rem;
      opacity:.7
    }

    /* Accesibilidad: respeta reduce-motion */
    @media (prefers-reduced-motion: reduce){
      *{
        animation: none !important;
        transition: none !important
      }
      .btn-principal:hover {
        transform: none;
        box-shadow: none;
      }
    }

    /* Responsive */
    @media (max-width: 768px) {
      .reset-done-container {
        margin: 1rem;
        padding: 2rem 1.5rem;
      }

      .egarage-logo h1 {
        font-size: 2.5rem;
      }
    }
  </style>
{% endblock %}

{% block body_class %}auth-page{% endblock %}

{% block content %}
  <div class="egarage-logo" aria-hidden="true">
    <h1>eGarage</h1>
    <div class="tagline">Workshop Management System</div>
  </div>

  <main class="reset-done-container" role="main" aria-labelledby="title">
    <h2 id="title">{% trans "Email sent successfully!" %}</h2>
    <p>
      {% blocktrans %}
      If an account exists with the email you entered, we've sent password reset instructions.
      Please check your inbox and follow the link to set a new password.
      {% endblocktrans %}
    </p>

    <section class="additional-info" aria-label="{% trans 'What to do next' %}">
      <h3>{% trans "What to do next:" %}</h3>
      <ul>
        <li>{% trans "Check your inbox (and spam folder)" %}</li>
        <li>{% trans "Open the email from eGarage" %}</li>
        <li>{% trans "Click the reset link" %}</li>
        <li>{% trans "Choose a new password" %}</li>
      </ul>
    </section>

    <p style="margin-top:1rem">
      <a class="btn-principal" href="{% country_url 'account_login' as_login_url %}{{ as_login_url|default:'/' }}">
        {% trans "Back to login" %}
      </a>
    </p>

    <p class="footer">
      {% trans "Need help? Contact our support team." %}
    </p>

    <p id="redirect-caption" class="footer" aria-live="polite">
      {% trans "Redirecting automatically in" %} <span id="countdown">5</span> {% trans "seconds…" %}
    </p>
  </main>
{% endblock %}

{% block extra_js %}
  <script>
    (function(){
      // Respeta reduce-motion para no "mover" a usuarios sensibles
      const reduce = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
      let countdown = 5;
      const el = document.getElementById('countdown');
      const target = "{% country_url 'account_login' as_login_url %}{{ as_login_url|default:'/' }}";

      if (reduce) return;  // sin auto-redirect si reduce-motion

      const t = setInterval(function(){
        countdown--;
        if (el) el.textContent = countdown;
        if (countdown <= 0) {
          clearInterval(t);
          window.location.href = target;
        }
      }, 1000);
    })();
  </script>
{% endblock %}
