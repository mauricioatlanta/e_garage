═══════════════════════════════════════════════════════════════
COMANDO PARA CREAR REGIONES DE CHILE EN EL SERVIDOR
═══════════════════════════════════════════════════════════════

Ejecuta este comando en el servidor para crear todas las regiones de Chile:

cd /home/atlantareciclajes/apps/egarage/current && \
python3 << 'PYEOF'
import os
import django
os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'gestion_taller.settings')
django.setup()

from taller.models.region_ciudad import TallerRegion

# Lista completa de las 16 regiones de Chile
regiones_chile = [
    "Arica y Parinacota",
    "Tarapacá",
    "Antofagasta",
    "Atacama",
    "Coquimbo",
    "Valparaíso",
    "Metropolitana de Santiago",
    "Libertador General Bernardo O'Higgins",
    "Maule",
    "Ñuble",
    "Biobío",
    "La Araucanía",
    "Los Ríos",
    "Los Lagos",
    "Aysén del General Carlos Ibáñez del Campo",
    "Magallanes y de la Antártica Chilena",
]

print("🔧 Creando regiones de Chile...\n")
print("=" * 70)

creadas = 0
existentes = 0

for nombre in regiones_chile:
    obj, created = TallerRegion.objects.get_or_create(nombre=nombre)
    if created:
        print(f"✅ Creada: {nombre}")
        creadas += 1
    else:
        print(f"ℹ️  Existente: {nombre}")
        existentes += 1

print()
print("=" * 70)
print(f"📊 Resumen:")
print(f"   ✅ Regiones creadas: {creadas}")
print(f"   ℹ️  Regiones existentes: {existentes}")
print(f"   📈 Total: {TallerRegion.objects.count()} regiones")
print("=" * 70)
print("\n✅ Proceso completado")
PYEOF

═══════════════════════════════════════════════════════════════

