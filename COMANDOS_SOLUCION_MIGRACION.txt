===========================================
SOLUCIÓN RÁPIDA: Error Migración Categoría
===========================================

PROBLEMA:
---------
django.db.utils.OperationalError: table "taller_categoriaservicio" already exists

SOLUCIÓN (Ejecutar en el servidor):
-----------------------------------

# 1. Ir al directorio del proyecto
cd /home/atlantareciclajes/apps/egarage/current

# 2. Activar entorno virtual
workon venv_egarage310

# 3. Verificar estado de migraciones
python manage.py showmigrations taller | head -20

# 4. Si la tabla ya existe, marcar migración inicial como aplicada
python manage.py migrate taller 0001_initial_migration --fake

# 5. Aplicar el resto de migraciones normalmente
python manage.py migrate

# 6. Verificar que todo está bien
python manage.py check
python manage.py showmigrations

===========================================
SOLUCIÓN ALTERNATIVA (Si la anterior no funciona):
===========================================

# Marcar todas las migraciones existentes como aplicadas
python manage.py migrate --fake-initial

# Luego aplicar solo las nuevas
python manage.py migrate

===========================================
VERIFICACIÓN:
===========================================

# Verificar que la tabla existe y tiene datos
python manage.py shell
>>> from taller.servicios.models import CategoriaServicio
>>> print(f"Categorías: {CategoriaServicio.objects.count()}")
>>> exit()

# Verificar estructura
python manage.py dbshell
.tables | grep categoria
.schema taller_categoriaservicio
.quit

===========================================

