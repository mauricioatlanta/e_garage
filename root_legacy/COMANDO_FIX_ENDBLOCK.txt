═══════════════════════════════════════════════════════════════
COMANDO PARA CORREGIR ERROR DE {% endblock %}
═══════════════════════════════════════════════════════════════

Ejecuta este comando en el servidor:

cd /home/atlantareciclajes/apps/egarage/current && \
cp templates/taller/us/en/dashboard/centro_operaciones_espacial.html templates/taller/us/en/dashboard/centro_operaciones_espacial.html.backup_endblock_$(date +%Y%m%d_%H%M%S) && \
python3 << 'PYEOF'
import re

file_path = "templates/taller/us/en/dashboard/centro_operaciones_espacial.html"

with open(file_path, 'r', encoding='utf-8') as f:
    lines = f.readlines()

# Buscar si el archivo tiene {% extends %}
has_extends = False
for line in lines:
    if '{% extends' in line:
        has_extends = True
        break

# Si no tiene extends, eliminar todos los {% block %} y {% endblock %}
new_lines = []
removed_count = 0

for i, line in enumerate(lines, 1):
    if not has_extends:
        # Si no hay extends, eliminar bloques
        if '{% block' in line or '{% endblock' in line:
            print(f"⚠️  Eliminando línea {i}: {line.strip()[:50]}")
            removed_count += 1
            continue
    
    new_lines.append(line)

# Escribir archivo corregido
with open(file_path, 'w', encoding='utf-8') as f:
    f.writelines(new_lines)

print(f"✅ Eliminadas {removed_count} líneas de bloques sin {% extends %}")
PYEOF

touch /var/www/www_egarage_cl_wsgi.py && \
echo "✅ Servidor reiniciado"

═══════════════════════════════════════════════════════════════

